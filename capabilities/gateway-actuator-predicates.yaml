naftiko: 1.0.0-alpha2
info:
  label: Spring Cloud Gateway Actuator API — Predicates
  description: 'Spring Cloud Gateway Actuator API — Predicates. 1 operations. Lead operation: Spring Cloud List all available
    route predicate factories. Self-contained Naftiko capability covering one Spring Cloud business surface.'
  tags:
  - Spring Cloud
  - Predicates
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SPRING_CLOUD_API_KEY: SPRING_CLOUD_API_KEY
capability:
  consumes:
  - type: http
    namespace: gateway-actuator-predicates
    baseUri: http://localhost:8080/actuator/gateway
    description: Spring Cloud Gateway Actuator API — Predicates business capability. Self-contained, no shared references.
    resources:
    - name: routepredicates
      path: /routepredicates
      operations:
      - name: getroutepredicates
        method: GET
        description: Spring Cloud List all available route predicate factories
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: gateway-actuator-predicates-rest
    port: 8080
    description: REST adapter for Spring Cloud Gateway Actuator API — Predicates. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/routepredicates
      name: routepredicates
      description: REST surface for routepredicates.
      operations:
      - method: GET
        name: getroutepredicates
        description: Spring Cloud List all available route predicate factories
        call: gateway-actuator-predicates.getroutepredicates
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: gateway-actuator-predicates-mcp
    port: 9090
    transport: http
    description: MCP adapter for Spring Cloud Gateway Actuator API — Predicates. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: spring-cloud-list-all-available
      description: Spring Cloud List all available route predicate factories
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: gateway-actuator-predicates.getroutepredicates
      outputParameters:
      - type: object
        mapping: $.
